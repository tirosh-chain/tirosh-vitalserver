[project]
name = "tirosh-vitalserver-core"
version = "0.2.0"
description = "Shared VitalServer domain models and policies."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tirosh_vitalserver"]
