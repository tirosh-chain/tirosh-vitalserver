# HostCLI runtime workflow boundary fragmentation

> ID: TS-048  
> Category: Architecture / Runtime workflow / macOS runtime  
> Owner: macOS runtime application layer  
> Status: resolved for HostCLI runtime fragmentation; Application usecase catalog completion remains follow-up

## Symptoms

- Runtime service restart, pkg install, update, rollback, repair, and health wait changes frequently touch many files across `Core`, `RuntimeWorkflow`, `HostCLI`, UI, tests, and docs.
- A small operational rule change, such as requiring `guest-log-sync` after runtime restart, needs edits in service policy, service controller, lifecycle workflow, install/update/rollback/repair paths, status document building, UI labels, and multiple tests.
- HostCLI runtime files still contain a mixture of command entrypoint composition, application usecase orchestration, workflow sequencing, launchd/process/filesystem effects, and status reporting.
- Release issues appear as different symptoms, but often share the same structural cause: an operation does not have one explicit owner for required state, effects, and completion criteria.
- The codebase has useful targets and policies, but contributors still need to inspect several layers to answer simple questions such as "who owns restarting this service?" or "what proves this workflow is complete?"

## Impact

- Changes have a wide blast radius and are hard to review.
- Release hardening becomes reactive because every field failure can require a new cross-layer patch.
- Race conditions are easier to introduce when lifecycle state, launchd state, VM state, disk state, and UI status are coordinated from multiple places.
- Recovery and update behavior can look correct in one path while another path misses the same required side effect.
- Tests tend to verify the latest bug path instead of enforcing a stable architectural contract.

## Cause

TS-043 introduced a useful `RuntimeWorkflow` boundary, but the current runtime implementation still lacks a stable application structure above the pure domain policies.

The current weak point is not "missing services objects." The weak point is that the responsibilities below are not consistently represented as separate, named layers:

```text
Bootstrap / composition root
  Wires concrete adapters and usecases.

Interface adapters
  Own launchd, process, filesystem, pkgutil, VM runner, guest HTTP, and log effects.

Application usecases
  Own one user-visible operation contract such as install, update, repair, uninstall,
  service restart, service health refresh, or disk repair.

Workflow
  Own operation order, persisted operation state, progress events, and completion gates.

Domain / Core
  Own pure policies, state machines, transition guards, invariants, and decisions.

Contracts
  Own explicit documents, commands, events, state values, and failure shapes.
```

When these roles are mixed inside `HostCLI/Runtime`, the code can still work, but the architecture stops localizing change. A new operational requirement then spreads through multiple workflow branches instead of being added to one usecase contract and one domain decision point.

## Checks

Use these checks before and during the refactor.

```sh
rg -n "RuntimeLifecycle|RuntimeServiceController|RuntimeServiceRestartPolicy|waitForHealth|restart.*Service|launchctl|pkgutil|FileManager|Process\\(" \
  apps/vitalserver-macos-runtime/Sources/HostCLI/Runtime \
  apps/vitalserver-macos-runtime/Sources/RuntimeWorkflow \
  apps/vitalserver-macos-runtime/Sources/Core

rg -n "struct .*UseCase|final class .*UseCase|protocol .*Port|Workflow|TransitionPolicy|StateMachine" \
  apps/vitalserver-macos-runtime/Sources/HostCLI/Runtime \
  apps/vitalserver-macos-runtime/Sources/RuntimeWorkflow \
  apps/vitalserver-macos-runtime/Sources/Core

rg -n "^import HostInfrastructure|^import HostCLI|^import MacHostRuntimeAdapter|^import MacRuntimeControlApp" \
  apps/vitalserver-macos-runtime/Sources/Core \
  apps/vitalserver-macos-runtime/Sources/RuntimeWorkflow
```

Review smell:

- `HostCLI` decides operation transitions instead of delegating to Core or RuntimeWorkflow.
- A workflow calls `launchctl`, `pkgutil`, `FileManager`, or process APIs directly instead of using an injected port.
- UI or status builders infer service state instead of formatting explicit provider state.
- One required runtime service is listed in more than one unrelated place without a single policy or contract explaining why.
- A successful command result is treated as proof of final state without a follow-up explicit observation.

## Actions

Proceed in small, behavior-preserving slices. Do not start with a broad folder rewrite.

1. Define the runtime application map.

   Record each operation as a usecase with an owner, input contract, output contract, required ports, workflow state, and completion condition.

   Target usecase catalog:

   ```text
   InstallRuntimeUseCase
   ApplyRuntimeUpdateUseCase
   RollbackRuntimeUpdateUseCase
   RepairRuntimeDatastoreUseCase
   RepairRuntimeVMDiskUseCase
   ControlRuntimeServicesUseCase
   RefreshRuntimeHealthUseCase
   UninstallRuntimeUseCase
   ```

   TS-048 introduced the first Application usecases for service control and health operations. The remaining user-visible operations still need dedicated Application usecase contracts in a follow-up refactor; this TS closes the HostCLI fragmentation slice, not the full Clean Architecture endpoint.

2. Stabilize service lifecycle first.

   Service lifecycle has caused repeated release-facing symptoms and is narrow enough to extract safely.

   Target shape:

   ```text
   Core
     RuntimeServiceRestartPolicy
     RuntimeRequiredServicePolicy

   RuntimeWorkflow / Application
     ControlRuntimeServicesUseCase
     RuntimeServiceLifecycleWorkflow
     RuntimeServiceLifecyclePorts

   HostCLI / adapters
     LaunchdRuntimeServiceAdapter
     RuntimeServiceStatusReader
     RuntimeLifecycle command composition
   ```

   The usecase should own "what operation is being requested." Core policy should own "which services are required." Adapters should own "how launchd is called."

3. Move completion gates into workflow contracts.

   Every operation that stops, starts, updates, repairs, or removes runtime state must name the explicit observations required before completion.

   Examples:

   - service restart is complete only after required services are explicitly observed loaded or running
   - update stop phase is complete only after VM lifecycle is explicitly stopped, not merely after a stop command returns
   - disk repair is complete only after repair result and disk attachability are explicitly observed
   - uninstall cleanup is complete only after artifacts and receipts are explicitly observed absent

4. Shrink `RuntimeLifecycle`.

   Keep it as a composition facade and command-facing coordinator. It may wire dependencies and call usecases. It should not own transition rules, completion gates, or direct external effects.

5. Add boundary tests before moving more behavior.

   Required tests:

   - transitional `Core` compatibility shims do not import host infrastructure, UI, CLI, or RuntimeControl layers.
   - external state/effect port protocols live in `Application/Ports`; `Core/Ports` only re-exports aliases while legacy imports remain.
   - `RuntimeWorkflow` does not import host infrastructure, UI, or CLI layers.
   - usecases expose explicit input/output contracts.
   - required service policy has one tested source of truth.
   - command success cannot be used as final state without explicit observation where the operation depends on external state.

6. Migrate one operation at a time.

   Recommended order:

   ```text
   ControlRuntimeServicesUseCase
   RefreshRuntimeHealthUseCase
   RepairRuntimeVMDiskUseCase
   ApplyRuntimeUpdateUseCase
   RollbackRuntimeUpdateUseCase
   InstallRuntimeUseCase
   UninstallRuntimeUseCase
   ```

   This order starts with the current service-status failures, then moves toward higher-risk update and disk behavior.

## Prevention

- New runtime behavior must enter through a named usecase, not through scattered HostCLI conditionals.
- A usecase must declare required ports and completion observations before implementation.
- Domain decisions must remain pure and testable.
- Adapters must return explicit typed results and failures; they must not convert dependency failure into empty/default success.
- UI must display explicit state and must not create operational state.
- Important release failures must add or update a troubleshooting entry with symptom, cause, fix direction, and prevention rule.

## Related Cases

- `TS-030`: Runtime state inference
- `TS-032`: macOS runtime explicit responsibility review
- `TS-039`: AGENTS.md fallback audit
- `TS-043`: RuntimeWorkflow target and layer boundary cleanup
- `TS-045`: Runtime install workflow state machine parity
- `TS-047`: Guest log sync service remains stopped after runtime restart

## Follow-up

- 2026-06-04: Created after repeated install, update, power-off, disk repair, launchd, and guest log sync failures showed that the remaining issue is structural fragmentation around HostCLI runtime workflows, not a single missing branch.
- 2026-06-05: Started Slice 1 for service lifecycle stabilization. Added the `Application` target with `ControlRuntimeServicesUseCase` and service lifecycle ports, moved service lifecycle command sequencing into `RuntimeWorkflow/ServiceLifecycle`, and introduced `RuntimeRequiredServicePolicy` as the required-service source of truth. HostCLI now wires concrete launchd/status observations into the workflow, while the workflow requires explicit service loaded/stopped observations before reporting service start/repair/stop completion. TS-048 remains active; next slice is runtime health refresh boundary cleanup.
- 2026-06-05: Completed Slice 2 for runtime health refresh. Added `RefreshRuntimeHealthUseCase` and `RuntimeWorkflow/HealthRefresh` so health snapshot classification, status message selection, and observed-event message selection no longer live in HostCLI. HostCLI now prints command output, maps explicit workflow health failures to the CLI error, and keeps Host-owned snapshot/status/event adapters wired at the boundary. Moved `RuntimeFailureReasonText` into Core so health workflows share one tested formatter. TS-048 remains active; next slice is health wait workflow boundary cleanup.
- 2026-06-05: Completed Slice 3 for runtime health wait. Added `WaitForRuntimeHealthUseCase` and `RuntimeWorkflow/HealthWait` so required service observations, wait polling, progress status, early failure, timeout, and healthy completion logging are no longer owned by HostCLI. HostCLI now wires launchd state, snapshots, status best-effort writes, sleep, and logging at the adapter boundary, then maps explicit workflow operation failure to the CLI runtime health failure. TS-048 remains active; next slice should target VM disk repair or update stop completion gates.
- 2026-06-05: Completed Slice 4 for VM disk repair. Moved VM disk repair orchestration into `RuntimeWorkflow/VMDiskRepair`, with HostCLI now providing command paths and concrete filesystem/process/service/status adapters at composition time. Added a replacement disk completion gate so repair does not restart services unless the replacement VM disk is explicitly present and at least the expected target size. TS-048 remains active; next slice should target update stop completion gates.
- 2026-06-05: Completed Slice 5 for apply-bundle step execution. Moved `RuntimeApplyBundleStepExecutor` into `RuntimeWorkflow/ApplyBundle`, so update stop sequencing, rootfs replacement, artifact/migration dispatch, service restart, activation, and health-wait step routing are no longer owned by HostCLI. Added workflow tests that keep direct stop distinct from guest-poweroff stop and prove guest shutdown preparation is cleared even when the observed VM stop gate fails. TS-048 remains active; next slice should target the broader apply-bundle workflow facade or rollback step execution.
- 2026-06-05: Completed Slice 6 for rollback step execution. Moved `RuntimeRollbackStepExecutor` into `RuntimeWorkflow/Rollback`, so rollback stop, rootfs restore, runtime-version restore, artifact restore, service restart, and health-wait step routing no longer depend on HostCLI types. Added RuntimeWorkflow tests that preserve artifact restore order, keep missing backup rootfs as an explicit failure, and reject non-rollback steps at the workflow boundary. TS-048 remains active; next slice should target the broader rollback workflow facade or rollback preflight boundary cleanup.
- 2026-06-05: Completed Slice 7 for rollback plan execution. Moved `RuntimeRollbackRunner` into `RuntimeWorkflow/Rollback`, so rollback plan selection, started/completed/failed progress events, recovering/healthy status writes, and preserved VM disk logging are no longer owned by HostCLI. Kept CLI command parsing outside RuntimeWorkflow by making the runner generic over the command input and requiring explicit preflight preparation through a port. TS-048 remains active; next slice should target rollback preflight or the broader rollback workflow facade.
- 2026-06-05: Completed Slice 8 for rollback preflight. Moved `BackupManifest` and `RuntimeRollbackCommand` into `Contracts`, then moved `RuntimeRollbackPreflightRunner` into `RuntimeWorkflow/Rollback`. Rollback backup selection, manifest-declared rootfs restore detection, missing backup directory failure, missing backup rootfs failure, and explicit restart policy capture are now workflow-level behavior with HostCLI supplying filesystem and manifest loading adapters. TS-048 remains active; next slice should target the rollback workflow facade.
- 2026-06-05: Completed Slice 9 for the rollback workflow facade. Moved `RuntimeRollbackWorkflow`, context, and operations into `RuntimeWorkflow/Rollback`, with HostCLI now only composing filesystem, launchd, service, status, and backup adapters. Added workflow-level integration tests for manifest loading, restart policy capture, rollback plan execution, step adapter dispatch, and missing manifest failure before side effects. Centralized rollback artifact file names in role-specific shared contracts instead of a generic runtime file-name bucket. TS-048 remains active; next slice should target the apply-bundle workflow facade or update preflight boundary.
- 2026-06-05: Completed Slice 10 for apply-bundle plan execution. Moved `RuntimeApplyBundleRunner` and `RuntimeWorkflowStatusReporter` into `RuntimeWorkflow`, so apply plan execution, progress publication, preflight failure status, rollback/degraded/critical handling, best-effort service restart after rollback failure, and cleanup best-effort logging are workflow-level behavior. HostCLI still owns bundle verification, staging, update preflight adapters, and concrete status/progress writers. TS-048 remains active; next slice should target apply-bundle preflight or the broader bundle workflow facade.
- 2026-06-05: Completed Slice 11 for apply-bundle preflight. Moved `RuntimeApplyBundlePreflightRunner` into `RuntimeWorkflow/ApplyBundle` and moved `RuntimeGuestCapabilityRequirement` into `Contracts`. Apply update staging input, manifest loading, compatibility checks, rootfs replacement detection, storage requirement calculation, explicit restart policy capture, guest disk-health gate, guest capability gates, and managed backup creation are now workflow-level behavior with HostCLI supplying concrete file, storage, health, guest, and backup adapters. TS-048 remains active; next slice should target the broader apply-bundle workflow facade.
- 2026-06-05: Completed Slice 12 for the apply-bundle workflow facade. Added `RuntimeApplyBundleWorkflow` in `RuntimeWorkflow/ApplyBundle` so apply log preparation, runner/preflight/step-executor composition, rollback/service/status wiring, and final mutable VM disk preservation logging are owned by the workflow boundary. HostCLI now delegates apply to that workflow and only supplies bundle staging/verification helpers plus concrete filesystem, service, guest, status, and artifact adapters. TS-048 remains active; next slice should separate bundle verification/staging or move the install workflow boundary.
- 2026-06-05: Completed Slice 13 for apply-bundle migration execution. Moved `RuntimeMigrationRunner` into `RuntimeWorkflow/ApplyBundle`, so migration ordering, executable-file gating, migration execution dispatch, and empty migration logging are no longer HostCLI-owned. HostCLI now supplies executable checks and command execution ports, while the workflow reports a typed workflow failure when a manifest-declared migration is not executable. TS-048 remains active; next slice should target artifact replacement or install step execution.
- 2026-06-05: Completed Slice 14 for apply-bundle artifact replacement. Moved `RuntimeArtifactReplacer` and artifact replacement destination/rule contracts into `RuntimeWorkflow/ApplyBundle`, so artifact payload validation, tar entry safety checks, replacement/extraction order, cleanup logging, and unsupported artifact failures are workflow-level behavior. HostCLI now supplies product-specific roots, runtime-tool allowlists, tar command, filesystem, and process ports explicitly. TS-048 remains active; next slice should target bundle verification/staging or install step execution.
- 2026-06-05: Completed Slice 15 for install step execution. Moved `RuntimeInstallStepExecutor` into `RuntimeWorkflow/Install` as a generic workflow step dispatcher, so install plan step routing and unsupported install-step failure no longer depend on HostCLI. HostCLI keeps ownership of concrete `InstallSettings` loading and maps those settings to an explicit `RuntimeServiceRestartPolicy` through the composition boundary. TS-048 remains active; next slice should target install directory/runtime effects or bundle verification/staging.
- 2026-06-05: Completed Slice 16 for guest update activation. Moved `RuntimeGuestActivationRunner` and `RuntimeGuestActivationWorkflow` into `RuntimeWorkflow/GuestActivation`, so guest deploy detection, activation request preparation, VM service start gate, result waiting, progress reporting, and activation failure mapping are no longer HostCLI-owned. HostCLI now supplies guest gateway effects, service observations, timestamps, status writers, polling sleep, and the activation wait timeout explicitly. TS-048 remains active; next slice should target datastore repair workflow or bundle verification/staging.
- 2026-06-05: Completed Slice 17 for datastore repair. Moved `RuntimeDatastoreRepairRunner`, `RuntimeDatastoreRepairResultWaiter`, and `RuntimeDatastoreRepairWorkflow` into `RuntimeWorkflow/DatastoreRepair`, so repair request preparation, VM start/restart selection, guest result waiting, proxy/watchdog restart, runtime health gate, progress status, and failure mapping are no longer HostCLI-owned. HostCLI now supplies guest gateway effects, launchd service effects, status writers, polling sleep, and the datastore repair wait timeout explicitly. TS-048 remains active; next slice should target install directory/runtime effects or bundle verification/staging.
- 2026-06-05: Completed Slice 18 for install directory preparation. Moved `RuntimeInstallDirectoryPreparer` into `RuntimeWorkflow/Install` as a generic preparation workflow, so install directory creation and stale guest-run document cleanup are no longer HostCLI-owned. HostCLI now supplies the concrete installed path list, custom VitalFiles path extraction from `InstallSettings`, and file-store create/existence/remove ports explicitly. TS-048 remains active; next slice should target remaining install runtime effects or bundle verification/staging.
- 2026-06-05: Completed Slice 19 for guest update shutdown preparation. Moved `RuntimeGuestShutdownRunner` and `RuntimeGuestShutdownWorkflow` into `RuntimeWorkflow/GuestShutdown`, so update shutdown request preparation, guest result waiting, progress reporting, timeout handling, and guest-reported failure mapping are no longer HostCLI-owned. HostCLI now supplies guest gateway effects, status writer, polling sleep, request metadata, and the shutdown wait timeout explicitly. TS-048 remains active; next slice should target remaining install runtime effects or bundle verification/staging.
- 2026-06-05: Completed Slice 20 for cloud-init seed creation. Moved `RuntimeCloudInitSeedWriter` into `RuntimeWorkflow/Install` with explicit seed context and filesystem/command/instance-id ports, so seed directory replacement, metadata/user-data generation, stale ISO removal, and ISO build dispatch are no longer HostCLI-owned. HostCLI now supplies concrete runtime paths, hdiutil path, file-store operations, and instance-id generation explicitly. TS-048 remains active; next slice should target remaining install VM provisioning, runtime config, permissions, or service start effects.
- 2026-06-05: Completed Slice 21 for install VM disk provisioning. Added `RuntimeInstallVMDiskProvisioner` in `RuntimeWorkflow/Install`, so rootfs-based VM disk creation, stale temporary disk cleanup, free-space gate calculation, disk move, creation logging, missing-rootfs failure, and final truncate command dispatch are no longer HostCLI-owned. HostCLI now supplies concrete rootfs/vmDisk paths, gunzip/truncate paths, file-store ports, storage guard, and logging explicitly. TS-048 remains active; next slice should target install runtime config, permissions, service start, or start-on-boot effects.
- 2026-06-05: Completed Slice 22 for install service start. Added `RuntimeInstallServiceStarter` in `RuntimeWorkflow/Install`, so start-after-install gating, sleep-prevention start, runtime service start order, and proxy-port cleanup before proxy start are no longer HostCLI-owned. HostCLI now supplies launchd start, proxy cleanup, and logging ports explicitly. TS-048 remains active; next slice should target install runtime config, permissions, or start-on-boot effects.
- 2026-06-05: Completed Slice 23 for install start-on-boot policy. Added `RuntimeInstallStartOnBootPolicyApplier` in `RuntimeWorkflow/Install`, so persisted start-on-boot setting and sleep-prevention launchd enable/disable decision are no longer HostCLI-owned. HostCLI now supplies launchctl path, persisted policy writer, and command execution port explicitly. TS-048 remains active; next slice should target install runtime config or permission effects.
- 2026-06-05: Completed Slice 24 for install permission configuration. Added `RuntimeInstallPermissionConfigurator` in `RuntimeWorkflow/Install`, so runtime/nginx ownership, proxy launchd port injection, and launch daemon plist chmod/chown ordering are no longer HostCLI-owned. HostCLI now supplies concrete paths, service plist list, command paths, and command execution port explicitly. TS-048 remains active; next slice should target install runtime config or bundle verification/staging.
- 2026-06-05: Completed Slice 25 for install settings cleanup. Added `RuntimeInstallSettingsCleaner` in `RuntimeWorkflow/Install`, so post-install settings removal is expressed as an explicit settings-file cleanup workflow instead of HostCLI inline file logic. HostCLI now supplies the default settings path and file-store existence/removal ports explicitly. TS-048 remains active; next slice should target install runtime config or bundle verification/staging.
- 2026-06-05: Completed Slice 26 for install VM runtime config mutation. Added generic `RuntimeInstallVMRuntimeConfigurator` in `RuntimeWorkflow/Install`, so required runtime directory creation, existing/default config selection, install settings mutation, shared-network bridge clearing, shared-directory/vital-files mapping, runtime-default application, encoding, and config write ordering are no longer HostCLI-owned. HostCLI keeps ownership of concrete `VMRuntimeConfig`, path constants, defaults, encoding, and file-store adapters through explicit ports. TS-048 remains active; next slice should target bundle verification/staging or fresh-install preflight boundaries.
- 2026-06-05: Completed Slice 27 for fresh-install preflight composition. Moved `RuntimeFreshInstallPreflightRunner` into `RuntimeWorkflow/Install`, so full-install preflight document assembly is now workflow-level behavior over explicit Host-provided settings, artifact, launchd service, package receipt, and proxy-port states. HostCLI keeps the concrete state readers for install settings files, filesystem artifact inspection, package receipts, launchd state, and `lsof` proxy-port checks. TS-048 remains active; next slice should target bundle verification/staging or remaining install executable/deploy environment effects.
- 2026-06-05: Completed Slice 28 for install executable preparation. Added `RuntimeInstallExecutablePreparer` in `RuntimeWorkflow/Install`, so installed executable chmod ordering is expressed as a workflow over explicit executable paths and a chmod command port instead of inline HostCLI install logic. HostCLI still owns the concrete binary/nginx paths and command executable. TS-048 remains active; next slice should target deploy environment writing or bundle verification/staging.
- 2026-06-05: Completed Slice 29 for bundle staging. Added `RuntimeBundleStager` in `RuntimeWorkflow/ApplyBundle`, so verified/materialized bundle staging now owns managed destination selection, destination cleanup, free-space gate ordering, copy dispatch, and staging completion logging through explicit filesystem/storage ports. HostCLI still owns bundle input materialization, archive validation, manifest loading, CLI printing, and concrete file-store adapters. TS-048 remains active; next slice should target bundle verification/materialization or deploy environment writing.
- 2026-06-05: Completed Slice 30 for bundle archive validation policy. Added pure `UpdateBundleArchiveVerifier` in Core, so archive listing path safety, single-root enforcement, and link-entry rejection no longer live in HostCLI. HostCLI still owns tar process execution and maps Core archive validation failures to the existing CLI bundle-verification error messages. TS-048 remains active; next slice should target bundle materialization or digest verification workflow boundaries.
- 2026-06-05: Completed Slice 31 for bundle checksum parsing. Added pure `UpdateBundleChecksumFileParser` in Core, so `checksums.txt` parsing is no longer inline HostCLI logic. HostCLI still owns reading the checksum file from the filesystem and passes the explicit text into Core parsing before digest verification. TS-048 remains active; next slice should target digest verification orchestration or bundle materialization.
- 2026-06-05: Completed Slice 32 for bundle digest verification orchestration. Added `RuntimeBundleDigestVerifier` in `RuntimeWorkflow/ApplyBundle`, so checksum start/completion logging, SHA/size read ordering, and Core digest verification are no longer inline HostCLI logic. HostCLI still owns concrete file reads and maps Core verification failures to existing CLI errors. TS-048 remains active; next slice should target bundle materialization or full bundle directory verification composition.
- 2026-06-05: Completed Slice 33 for bundle directory verification composition. Added `RuntimeBundleDirectoryVerifier` in `RuntimeWorkflow/ApplyBundle`, so required bundle documents, manifest plan creation, checksum loading, artifact digest/payload checks, migration digest checks, and verification logging order are no longer inline HostCLI logic. HostCLI still owns concrete filesystem gates, manifest/checksum reads, payload validation adapters, CLI printing, and existing `LauncherError` mapping. TS-048 remains active; next slice should target bundle materialization.
- 2026-06-05: Completed Slice 34 for bundle materialization. Added `RuntimeBundleMaterializer` in `RuntimeWorkflow/ApplyBundle`, so directory-vs-archive selection, temporary extraction directory creation, archive listing/entry validation, tar extraction dispatch, extracted-directory gate, and extraction logging are no longer inline HostCLI logic. HostCLI still owns concrete tar path, temp root generation, filesystem/process adapters, cleanup callback, and existing `LauncherError` mapping. TS-048 remains active; next slice should reassess remaining HostCLI runtime workflow responsibilities.
- 2026-06-05: Completed Slice 35 for bundle preparation composition. Added `RuntimeBundlePreparationWorkflow` and later moved it into `Workflow/RuntimeUpdateLifecycle`, so verify/stage now share one explicit `materialize -> verify -> optional stage -> cleanup` sequence and reuse the verified manifest version for staging. HostCLI still owns CLI printing, concrete filesystem/process adapters, tar/error mapping, and artifact replacement adapters. TS-048 remains active; next slice should reassess whether remaining `RuntimeBundleWorkflow` code is adapter composition or still owns operation policy.
- 2026-06-05: Completed Slice 36 for guest runtime config contract ownership. Moved `GuestRuntimeConfigDocument` and `GuestRuntimeSettingsDocument` into `Contracts`, keeping HostCLI-only `load/default` helpers beside Host file-store and constants ownership. Added contract tests for decode, legacy URL migration, required explicit fields, and settings projection. TS-048 remains active; next slice should reassess remaining HostCLI runtime documents and composition-only facades.
- 2026-06-05: Completed Slice 37 for runtime version document ownership. Moved applied/installed `runtime-version.json` document types into `Contracts`, leaving `RuntimeVersionStore` in HostCLI as the explicit filesystem owner. Added contract round-trip tests for both version document shapes. TS-048 remains active; next slice should reassess whether remaining HostCLI document types are private settings inputs or shared runtime contracts.
- 2026-06-05: Completed Slice 38 for observed event type policy ownership. Moved pure event-type selection from HostCLI into `Core/Policy/RuntimeObservedEventTypePolicy`, so VM/domain error precedence is tested as domain policy while HostCLI only records events through adapters. TS-048 remains active; next slice should reassess remaining HostCLI policies for domain decisions versus adapter-only behavior.
- 2026-06-05: Completed Slice 39 for Redis backup workflow ownership. Moved Redis backup request/result documents into `Contracts` and later moved `RuntimeRedisBackupWorkflow` into `Workflow/RuntimeRepairLifecycle`, so request preparation, VM start gate, explicit result polling, stale result handling, status writes, timeout, and failure mapping no longer live inline in `RuntimeLifecycle`. HostCLI still owns concrete file IO, launchd/service adapters, CLI printing, and mapping workflow operation failures to `LauncherError`. TS-048 remains active; next slice should reassess the remaining `RuntimeLifecycle` methods for inline operation workflows.
- 2026-06-05: Completed Slice 40 for HostCLI composition naming. Renamed the remaining HostCLI install/bundle `Workflow` facades to `RuntimeInstallComposition` and `RuntimeBundleComposition`, including tests and `RuntimeLifecycle` call sites. This keeps HostCLI searchable as composition/adapters instead of implying it owns workflow rules.
- 2026-06-05: Completed Slice 41 for configure workflow ownership. Added `RuntimeWorkflow/Configure/RuntimeConfigureWorkflow`, moved configure validation, VM/guest config mutation, settings projection, restart decision, and status-preserving invalid argument failures out of HostCLI. HostCLI now parses CLI arguments, maps them to workflow input, supplies config/file/secret/service ports, and maps workflow invalid arguments back to the existing CLI error shape.
- 2026-06-05: Completed Slice 42 for watchdog workflow ownership. Moved watchdog orchestration into `RuntimeWorkflow/Watchdog`, with recovery wait and human output injected as context/ports. HostCLI now supplies log preparation, health snapshots, proxy liveness, restart effects, lifecycle/status/event writers, sleep, and printing from the composition boundary.
- 2026-06-05: TS-048 closeout audit passed. `Core`, `Application`, and `RuntimeWorkflow` do not import HostCLI, HostInfrastructure, Mac adapter, or UI layers. `HostCLI/Runtime` has no remaining `*Policy`, `*Document`, or `*Workflow` declarations in the architectural audit search. Remaining HostCLI runtime code is command parsing, composition, concrete state readers/writers, process/filesystem/launchd adapters, and CLI error/output mapping. RuntimeWorkflow command references such as `runProcess`/`launchctl` are injected ports or explicit context values, not direct process ownership. Full macOS runtime verification passed with 995 tests and 0 failures. TS-048 is resolved for HostCLI runtime fragmentation; remaining operation-level Application usecase catalog work should be tracked as narrower follow-up TS entries instead of reopening this fragmentation issue.
- 2026-06-05: Review follow-up clarified the TS-048 completion scope, aligned advertised service URL validation across UI and configure workflow, restored explicit HTTP default public-port behavior, hardened update bundle tar entry validation to reject non-file/non-directory entries, and moved uninstall open-file diagnostics command ownership out of workflow code into explicit diagnostics context.
- 2026-06-05: Follow-up architecture migration is tracked in GitHub issue #47, `macOS runtime을 이상적 레이어 구조로 수렴`. TS-048 remains the HostCLI/Runtime fragmentation closeout record; #47 owns the broader Clean Architecture/DDD target shape, including future `Domain`, `Application`, `Workflow`, `Infrastructure`, `HostAdapters`, `Interfaces`, and `Bootstrap` layer convergence. See `docs/runtime/macos/ideal-layer-migration.md` for the migration runway and first recommended slice.
- 2026-06-05: Started #47 skeleton-first migration. Added ideal layer targets and roots for `Domain`, `Workflow`, `Infrastructure`, `HostAdapters`, `Interfaces`, and `Bootstrap`, and expanded boundary tests to require the target graph and folder skeleton before more operation moves. Added `ConfigureRuntimeUseCase` as the first #47 Application boundary; HostCLI configure now maps CLI input into the usecase request and supplies the existing configure workflow through an explicit port.
- 2026-06-05: Advanced the first #47 operation migration by moving `RuntimeConfigureWorkflow` into `Workflow/RuntimeConfigureLifecycle`. `RuntimeSharedDirectoryConfiguration` now lives in `Contracts` because both install and configure workflows consume the same explicit shared-directory contract during the transition.
- 2026-06-05: Extracted advertised service URL validation and compatibility public endpoint selection into `Domain/Policies/RuntimeAdvertisedURLPolicy`, with focused Domain tests. The configure workflow now consumes the pure Domain decision instead of owning URL policy logic.
- 2026-06-05: Moved `RuntimeServiceLifecycleWorkflow` into `Workflow/RuntimeServiceLifecycle` and extracted loaded/stopped service completion checks into `Domain/Policies/RuntimeServiceLifecycleCompletionPolicy`. HostCLI service control now composes the final Workflow target while the Domain policy keeps missing, read-failed, permission-denied, loaded, and not-loaded states distinct.
- 2026-06-05: Moved health wait pure policy into `Domain/Policies`, kept `Core` compatibility shims for existing callers, removed the `Application -> Core` target dependency, and moved `RuntimeHealthWaitWorkflow` into `Workflow/RuntimeHealth`. HostCLI health wait now maps the final Workflow error while Domain keeps required-service pending states, missing snapshot failure reasons, and early guest bootstrap failure distinct.
- 2026-06-05: Moved `RuntimeHealthRefreshWorkflow` into `Workflow/RuntimeHealth`, so both refresh and wait health operations now live in the final Workflow target. HostCLI health check composes the final workflow and maps the final health refresh workflow error to the existing CLI failure shape.
- 2026-06-05: Moved `RuntimeInstallDirectoryPreparer`, `RuntimeInstallSettingsCleaner`, `RuntimeInstallServiceStarter`, and `RuntimeInstallVMRuntimeConfigurator` into `Workflow/RuntimeInstallLifecycle`. These install units already depended only on explicit contracts/Foundation values and injected operation closures, so they can live in the final Workflow target while concrete command/hdiutil/chmod/launchctl units remain in HostCLI adapter/composition code.
- 2026-06-05: Extracted the install start-on-boot sleep-prevention enable/disable decision into `Domain/Policies/RuntimeInstallStartOnBootPolicy`, then moved `RuntimeInstallStartOnBootPolicyApplier` into `HostAdapters/Launchd`. HostCLI now composes the persisted setting writer and concrete service-manager command port, but no longer owns the pure rule or the adapter type.
- 2026-06-05: Moved `RuntimeInstallExecutablePreparer` into `HostAdapters/Process` and `RuntimeInstallPermissionConfigurator` into `HostAdapters/Launchd`. HostCLI install composition still supplies concrete paths and command execution ports, but chmod/chown/plist command ordering now lives in adapter targets instead of the CLI source tree.
- 2026-06-05: Moved `RuntimeCloudInitSeedWriter` into `HostAdapters/CloudInit` and `RuntimeInstallVMDiskProvisioner` into `HostAdapters/Process`. HostCLI still composes concrete runtime paths, file-store, process runner, storage guard, and logging ports, while cloud-init document/ISO assembly and rootfs-to-disk provisioning order live outside the CLI source tree. The VM disk provisioner now reports a HostAdapters-local missing-file error instead of depending on `LauncherError`.
- 2026-06-05: Added `Application/UseCases/InstallRuntime/InstallRuntimeUseCase` as the install operation intent boundary. HostCLI install composition now maps CLI install/install-provision entrypoints into an explicit Application request and adapts that request to the transitional `RuntimeInstallWorkflow` command, keeping `Application` free of `Core` plan/status workflow details.
- 2026-06-05: Moved `RuntimeOperationPlan` into `Domain/Models` and `RuntimeInstallTransitionPolicy` into `Domain/StateMachines`, with `Core` retaining typealias-only compatibility shims for existing imports. Install step order, install/provision distinction, preflight/payload blocker handling, and step transition legality are now tested as Domain behavior while transitional `RuntimeInstallWorkflow` continues to execute and persist the Domain decisions.
- 2026-06-05: Moved `RuntimeStepExecutionEvent` into `Contracts` and `RuntimeInstallStepExecutor` into `Workflow/RuntimeInstallLifecycle`. Install step dispatch now depends on Domain restart policy and explicit injected effects without importing `Core` or transitional `RuntimeWorkflow`.
- 2026-06-05: Moved `RuntimeInstallWorkflow` into `Workflow/RuntimeInstallLifecycle` with an install-specific workflow error. The workflow now consumes Domain transition decisions directly and writes state/status/progress through explicit ports without importing `Core` or transitional `RuntimeWorkflow`.
- 2026-06-05: Moved `RuntimeFreshInstallPreflightPolicy` and `RuntimeUninstallReadinessPolicy` into `Domain/Policies`, with `Core` retaining typealias-only compatibility shims. Then moved `RuntimeFreshInstallPreflightRunner` into `Workflow/RuntimeInstallLifecycle`, so fresh-install preflight assembly now lives in the final Workflow layer and consumes Domain preflight policy over explicit Host-provided state. `RuntimeWorkflow/Install` no longer contains Swift implementation files.
- 2026-06-05: Moved `RuntimeUninstallTransitionPolicy` into `Domain/StateMachines`, with `Core` retaining typealias-only compatibility shims. This made uninstall transition/readiness types consumable from the final Workflow layer without importing `Core`.
- 2026-06-05: Moved `RuntimeProcessResult` into `Contracts`, with `Core` retaining a compatibility alias in `RuntimeCommandRunner`. This keeps injected process command results as explicit shared contracts so workflow code does not import `Core` only to format host adapter command output.
- 2026-06-05: Moved `RuntimeUninstallWorkflow` into `Workflow/RuntimeUninstallLifecycle`. Uninstall orchestration now lives in the final Workflow layer and consumes Domain transition decisions plus explicit Host-provided state/effect ports without importing `Core` or transitional `RuntimeWorkflow`.
- 2026-06-05: Moved `RuntimeRedisBackupWorkflow` into `Workflow/RuntimeRepairLifecycle` with a Redis-backup-specific workflow error. Redis backup orchestration now lives in the final Workflow layer without importing `Core` or transitional `RuntimeWorkflow`.
- 2026-06-05: Moved `RuntimeBundlePreparationWorkflow` into `Workflow/RuntimeUpdateLifecycle`. Bundle verify/stage preparation now lives in the final Workflow layer without importing `Core` or transitional `RuntimeWorkflow`.
- 2026-06-05: Moved `RuntimeBundleStager` into `Workflow/RuntimeUpdateLifecycle`. Managed bundle staging order now lives in the final Workflow layer while HostCLI supplies concrete file-store and storage ports.
- 2026-06-05: Moved `UpdateBundleVerifier` and `UpdateBundleArchiveVerifier` into `Domain/Policies`, with `Core` retaining typealias-only compatibility shims. Then moved `RuntimeBundleDigestVerifier`, `RuntimeBundleDirectoryVerifier`, and `RuntimeBundleMaterializer` into `Workflow/RuntimeUpdateLifecycle`, so digest verification, required bundle document checks, archive root/type validation, and materialization sequencing no longer live in HostCLI or import transitional `Core`.
- 2026-06-05: Moved `RuntimeArtifactReplacer` into `Workflow/RuntimeUpdateLifecycle`, with a workflow-local `RuntimeArtifactReplacementError`. Artifact payload validation, tar entry safety checks, replacement/extraction order, and temporary validation cleanup logging now live in final Workflow without importing `Core` or transitional `RuntimeWorkflow`; HostCLI supplies only concrete destinations, tar command, filesystem, process, and log ports.
- 2026-06-05: Moved `RuntimeMigrationRunner` into `Workflow/RuntimeUpdateLifecycle`, with a workflow-local `RuntimeMigrationRunnerError`. Migration executable gating, empty-migration logging, ordered migration dispatch, and non-executable migration failure now live in final Workflow without importing transitional `RuntimeWorkflow`; HostCLI supplies only executable-file inspection, process execution, and log ports.
- 2026-06-05: Moved `RuntimeRecoveryPlanner` and `RuntimeWatchdogRecoveryPolicy` into `Domain/Policies`, with `Core` retaining typealias-only compatibility shims. Then moved `RuntimeWatchdogRunner` into `Workflow/RuntimeWatchdog`, so watchdog health observation, recovery suppression/deferral, recovery action dispatch, lifecycle finalization, status writes, and event writes live in final Workflow while HostCLI supplies only concrete health, liveness, service, lifecycle, sleep, log, and print ports.
- 2026-06-05: Moved `RuntimeGuestDocumentLoadResult` into `Contracts`, with `Core` retaining a typealias through `RuntimeGuestGateway`. Moved `GuestActivationEvaluator` and `GuestShutdownEvaluator` into `Domain/Policies`, with `Core` retaining typealias-only compatibility shims. Then moved `RuntimeGuestActivationRunner`, `RuntimeGuestActivationWorkflow`, `RuntimeGuestShutdownRunner`, and `RuntimeGuestShutdownWorkflow` into `Workflow/RuntimeUpdateLifecycle`, so guest update request preparation, result polling, progress reporting, timeout handling, and guest-reported failure mapping live in final Workflow while HostCLI supplies only concrete guest gateway, service, timestamp, status, sleep, and log ports.
- 2026-06-05: Moved `DatastoreRepairEvaluator` into `Domain/Policies`, with `Core` retaining typealias-only compatibility shims. Then moved `RuntimeDatastoreRepairResultWaiter`, `RuntimeDatastoreRepairRunner`, and `RuntimeDatastoreRepairWorkflow` into `Workflow/RuntimeRepairLifecycle`, so datastore repair request preparation, guest result polling, VM start/restart selection, proxy/watchdog restart, runtime health gate, and status writes live in final Workflow while HostCLI supplies only concrete guest gateway, launchd/service, status, sleep, and log ports.
- 2026-06-05: Moved `RuntimeVMDiskRepairRunner` into `Workflow/RuntimeRepairLifecycle`, with a VM-disk-repair-specific workflow error. VM disk replacement ordering, archive-before-replace behavior, replacement disk verification, service restart, and health gate sequencing now live in final Workflow without importing `Core` or transitional `RuntimeWorkflow`; HostCLI supplies only concrete filesystem, process, storage, backup, service, status, timestamp, and log ports.
- 2026-06-05: Moved `RuntimeUpdateCompatibilityChecker`, `RuntimeUpdatePreflightPolicy`, `ApplyBundlePreflightContext`, and `RuntimeOperationPlanRunner` into `Domain`, with `Core` retaining typealias-only compatibility shims. Then moved `RuntimeApplyBundlePreflightRunner`, `RuntimeApplyBundleRunner`, `RuntimeApplyBundleStepExecutor`, `RuntimeApplyBundleWorkflow`, and `RuntimeWorkflowStatusReporter` into final `Workflow`, so apply-bundle preflight gates, step dispatch, progress publication, rollback/degraded/critical handling, and facade composition no longer import `Core` or transitional `RuntimeWorkflow`.
- 2026-06-05: Moved `RollbackPreflightContext` into `Domain/Models`, with `Core` retaining a typealias-only compatibility shim. Then moved `RuntimeRollbackPreflightRunner`, `RuntimeRollbackRunner`, `RuntimeRollbackStepExecutor`, and `RuntimeRollbackWorkflow` into `Workflow/RuntimeUpdateLifecycle`, with a rollback-specific workflow error. After rollback moved, the transitional `RuntimeWorkflow` target and remaining shared error/helper files were removed from SwiftPM.
- 2026-06-05: Moved the remaining pure health/bootstrap/status/event/update support from `Core` into `Domain`: `GuestBootstrapEvaluator`, `RuntimeHealthEvaluator`, `RuntimeVMHealthPolicy`, `RuntimeStatusDocumentBuilder`, `RuntimeObservedEventTypePolicy`, `RuntimeInstallProvisionPayloadPolicy`, `RuntimeManagedOperationPolicy`, and `UpdateBundleChecksumFileParser`. `Core` now retains typealias-only compatibility shims for these APIs.
- 2026-06-05: Moved external state/effect port protocols from `Core/Ports` into `Application/Ports`, including command, guest gateway, file store, storage usage, timing, HTTP probe, service manager, status repository, and event repository contracts. `Core/Ports` now only re-exports aliases for existing callers, so port ownership sits with Application while concrete HostInfrastructure/HostCLI adapters continue to report explicit typed results.
- 2026-06-05: Moved host filesystem/repository/observability implementations from `HostInfrastructure` into final `Infrastructure` responsibility folders. `SystemRuntimeFileStore`, storage usage, installed paths, JSON status/guest gateway, JSONL/SQLite event repositories, SQLite observability store, VitalDB observation repository, and relationship projection planner now live in `Infrastructure`; `HostInfrastructure` retains typealias-only compatibility shims for existing callers.
- 2026-06-05: Moved Runtime Control HTTP/API boundary, wire codec, local loopback server, static file responder, client read handler, dev console, and test-kit router implementation from transitional `RuntimeControlAPI` into `Interfaces/RuntimeControlAPI`. The `RuntimeControlAPI` target now keeps a typealias-only compatibility surface for existing imports, while the final Interfaces layer owns API input mapping and transport formatting.
- 2026-06-05: Moved concrete process, curl, launchd, and timing adapters from transitional `HostCLI` into final `HostAdapters`: `SystemRuntimeCommandRunner`, `CurlRuntimeHTTPProber`, `LaunchdRuntimeServiceManager`, `SystemRuntimeClock`, and `ThreadRuntimeSleeper`. `HostCLI/Runtime` keeps typealias-only shims for those names while CLI code continues to act as an entrypoint and composition boundary.
- 2026-06-05: Moved `RuntimeControlStatusAnnotator` from transitional `MacRuntimeControlApp/Composition` into `Interfaces/MacRuntimeControlApp`. Runtime Control app status annotation now lives with API/UI boundary read-model formatting, while the app target keeps a typealias shim during the gradual `MacRuntimeControlApp -> Interfaces/Bootstrap` split.
- 2026-06-05: Moved `RuntimeActiveOperationPolicy` and `RuntimeBackupSelectionPolicy` from transitional `MacRuntimeControlApp/Presentation/Policies` into `Interfaces/MacRuntimeControlApp`. These policies interpret explicit RuntimeControl/Contracts read models for UI state and backup selection without reading Host state, so the app target now keeps typealias shims while remaining SwiftUI/AppKit and native shell code stays transitional.
- 2026-06-05: Moved `RuntimeViewModelBackupActionPlanner` and its plan/failure value types into `Interfaces/MacRuntimeControlApp`. Backup rollback/delete action planning now consumes explicit selected path and reported backups root without reading Host state; `MacRuntimeControlApp` keeps only typealias shims plus app-local failure message formatting.
- 2026-06-05: Moved `RuntimeViewModelTestKitStatePolicy` and `RuntimeViewModelTestKitStartInput` into `Interfaces/MacRuntimeControlApp`. Test-kit start/stop gates, available-bed selection, and operator input normalization now consume explicit RuntimeControl test-kit status without touching SwiftUI, AppKit, native shell, or Host state.
- 2026-06-06: Moved `RuntimeViewModelObservabilityRefresher`, `RuntimeEventPeriodOption`, and refresh result/loader contracts into `Interfaces/MacRuntimeControlApp`. Runtime event and VitalDB observability refresh now consumes explicit snapshot-loader results and deterministic time input, while `MacRuntimeControlApp` keeps only typealias shims, app-local period title formatting, and the concrete snapshot-loader conformance.
- 2026-06-06: Moved `RuntimeRecorderActivityChartDataBuilder` and recorder activity display/bucket value types into `Interfaces/MacRuntimeControlApp`. Recorder activity timeline projection now stays outside the SwiftUI chart view and preserves read failure, not-reported, empty, and no-bucket states explicitly; the app target keeps typealias shims plus local title and axis label formatting.
- 2026-06-06: Moved `RuntimeViewModelStatusRefresher`, its refresh result, and status snapshot/formatting contracts into `Interfaces/MacRuntimeControlApp`. Status refresh now orchestrates explicit status snapshot reads and busy/file-backed operation presentation in the Interfaces layer, while `MacRuntimeControlApp` keeps the concrete snapshot-loader conformance and app-local `RuntimePresentationFormatter` copy mapping.
- 2026-06-06: Moved `RuntimeHealthNotificationState` into `Interfaces/MacRuntimeControlApp`. Notification state classification now consumes explicit RuntimeControl status/readiness state without owning notification side effects or app-local notification copy; `RuntimeHealthNotificationCoordinator` keeps those app concerns.
- 2026-06-06: Moved `RuntimeEventDisplayPolicy` into `Interfaces/MacRuntimeControlApp` behind an explicit `RuntimeEventDisplayVocabulary` contract. Runtime event row projection now consumes explicit event documents and app-provided copy/label formatting instead of reading `AppConstants` directly from the policy implementation.
- 2026-06-06: Moved `RuntimeStatusUptimeFormatter` into `Interfaces/MacRuntimeControlApp`. Status display uptime calculation now handles explicit uptime seconds, Docker `startedAt`, observation timestamps, and nanosecond ISO8601 normalization outside the app display policy; `RuntimeStatusDisplayPolicy` keeps app-local labels and status copy.
- 2026-06-06: Moved `RuntimeStatusReachabilityPolicy` into `Interfaces/MacRuntimeControlApp`. HTTP reachability, launchd service severity, update-in-progress service masking, and VM state severity now consume explicit status values in the Interfaces layer; `RuntimeStatusDisplayPolicy` keeps app-local labels and status copy mapping.
- 2026-06-06: Moved `RuntimeVitalRecorderDisplayPolicy` into `Interfaces/MacRuntimeControlApp`. VitalDB recorder/bed status labels, status tone, patient connection text, missing reported values, recorder anomaly text, and activity data-rate text now stay out of SwiftUI views; the app panels keep layout and color mapping only.
- 2026-06-06: Moved `RuntimeProcessMessageFormatter` into `Interfaces/MacRuntimeControlApp`. Command result stdout/stderr/exit-code projection now lives at the API/UI boundary, with the app target retaining only the transitional typealias while command action runners consume explicit `RuntimeCommandResult` values.
- 2026-06-06: Moved `RuntimeVitalFilesDirectoryPolicy` into `Interfaces/MacRuntimeControlApp`. Vital files directory input validation now stays as a pure API/UI boundary policy that rejects missing, relative, protected user folders, and iCloud Drive paths without reading filesystem state.
- 2026-06-06: Moved `RuntimeSection` into `Interfaces/MacRuntimeControlApp`. Runtime Control section ordering, grouping, and test-tab visibility now live as API/UI boundary policy; `MacRuntimeControlApp` keeps only generated test-enabled defaults and app-local section titles.
- 2026-06-06: Moved `RuntimeSettingsValidator` and `RuntimeSettingsValidationResult` into `Interfaces/MacRuntimeControlApp`. Runtime settings validation now consumes explicit current/installed settings and pure input policies without reading Host state; `MacRuntimeControlApp` keeps only the transitional aliases.
- 2026-06-06: Moved `RuntimePresentationFormatter` into `Interfaces/MacRuntimeControlApp` behind a `RuntimePresentationVocabulary` contract. Presentation formatting now consumes explicit status/settings/backup inputs and injected UI vocabulary instead of reading app constants directly; `MacRuntimeControlApp` keeps the AppConstants-backed vocabulary adapter and the transitional aliases.
- 2026-06-06: Split log export destination validation so `RuntimeLogExportDestinationPolicy` in `Interfaces/MacRuntimeControlApp` owns only pure URL/path rule decisions over explicit facts. The `MacRuntimeControlApp` adapter still owns `FileManager` reads for existing-directory and writable-directory facts, preventing Host filesystem state from leaking inward as hidden fallback.
- 2026-06-06: Moved recorder summary projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusRecorderSummaryPolicy`. The policy now consumes explicit Runtime status/container observation read models and an injected not-reported vocabulary; `RuntimeStatusDisplayPolicy` keeps app-local labels and the broader status display vocabulary for later slices.
- 2026-06-06: Moved action-needed decision policy from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusActionNeededPolicy`. The policy now owns readiness/managed-operation suppression, primary failure reason selection, read-issue handling, and recovery-action text selection over explicit `RuntimeStatus`; the app keeps UI severity/color mapping and copy vocabulary injection.
- 2026-06-06: Moved remote-console availability projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusRemoteConsoleAvailabilityPolicy`. The policy now owns explicit runtime-control HTTP reachability and uptime formatting over `RuntimeStatus`; the app keeps only copy vocabulary injection and UI severity/value mapping, preventing reachability rules from drifting back into SwiftUI-facing code.
- 2026-06-06: Moved VM-state display projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusVMStatePolicy`. VM state text and severity now come from an injected vocabulary plus explicit `RuntimeVMState`; the app target keeps only AppConstants-backed copy and UI value mapping, so VM-state presentation does not recreate domain/runtime state from absence.
- 2026-06-06: Moved overall-health projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusOverallHealthPolicy`. Readiness, update-in-progress precedence, installation state, runtime lifecycle state, and unknown-state display now consume explicit `RuntimeStatus` in the Interfaces layer; the app keeps only AppConstants-backed copy and UI severity/value mapping.
- 2026-06-06: Moved HTTP reachability label projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusReachabilityLabelPolicy`. The app no longer maps raw HTTP status strings to operator copy directly; it injects display vocabulary while the Interfaces policy preserves not-reported, reachable, unavailable, unreachable, and failed as distinct display states.
- 2026-06-06: Moved launchd service value projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusServiceValuePolicy`. Typed service states, fallback loaded booleans, unavailable state, and update-in-progress masking now produce explicit service display values in Interfaces; the app keeps only launchd copy vocabulary and UI severity/value mapping.
- 2026-06-06: Moved HTTP service value projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusHTTPValuePolicy`. HTTP status labels, HTTP severity, uptime preservation, and update-in-progress masking now produce explicit HTTP display values in Interfaces; the app keeps only display vocabulary injection, service action wiring, and UI severity/value mapping.
- 2026-06-06: Moved compose service value projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusComposeServiceValuePolicy`. Container health, container state, not-reported state, severity, and uptime now come from explicit container observation inputs in Interfaces; the app keeps only compose service ids, copy vocabulary, actions, and UI severity/value mapping.
- 2026-06-06: Moved health detail item projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusHealthDetailsPolicy`. Runtime install state, VM state/errors, explicit failure reasons, status read issues, VM IP, service reachability, compose health, and watchdog service details now produce explicit detail items in Interfaces; the app keeps only labels/copy vocabulary and UI item mapping.
- 2026-06-06: Moved advanced service health projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusAdvancedServiceHealthPolicy`. Launchd service rows, HTTP service rows, compose service rows, uptime, update-in-progress masking, and presentation action ids now come from explicit runtime status and container observation inputs in Interfaces; the app maps only action ids and UI values.
- 2026-06-06: Moved Vital Server availability summary projection from `RuntimeStatusDisplayPolicy` into `Interfaces/MacRuntimeControlApp/RuntimeStatusVitalServerAvailabilityPolicy`. Update-in-progress text, runtime-not-installed text, host proxy reachability, HTTP severity, and app container uptime now come from explicit runtime status and container observation inputs in Interfaces; the app keeps only copy vocabulary and UI value mapping.
- 2026-06-06: Moved status document reporting/writing orchestration from `HostCLI/Runtime` into `Workflow/RuntimeShared/RuntimeStatusReporter` and `RuntimeStatusWriter`. The final Workflow types take `productIdentifier` explicitly from HostCLI composition instead of reading HostCLI constants, and HostCLI keeps typealias-only shims for the transitional names.
- 2026-06-06: Moved runtime event document creation, observation event publishing, observed status publishing, and best-effort event recording from `HostCLI/Runtime` into `Workflow/RuntimeShared`. `RuntimeEventFactory`, `RuntimeObservationRecorder`, `RuntimeEventPublisher`, `RuntimeObservedEventPublisher`, and `RuntimeObservedStatusPublisher` now consume explicit status/snapshot/progress inputs plus Application event-recording ports; HostCLI only composes concrete repositories, status readers, projectors, timestamps, product identifiers, and logging.
- 2026-06-06: Moved `RuntimeVitalDBObservationProjector` from `HostCLI/Runtime` into `Workflow/RuntimeShared`. VitalDB observation projection now consumes an explicit observation plus an injected append operation and log sink; HostCLI only wires the concrete SQLite repository closure.
- 2026-06-06: Removed the duplicate `HostCLI/Runtime/RuntimeBestEffortRecording` helper. Best-effort status, progress, and observed-event write failure logging now lives only in `Workflow/RuntimeShared/RuntimeWorkflowBestEffortRecording`, keeping workflow-owned status/event recording policy out of HostCLI while HostCLI continues to inject concrete write closures and log sinks.
- 2026-06-06: Moved `RuntimePackageReceiptStateReader` from `HostCLI/Runtime` into `Infrastructure/PackageReceipts`. The reader owns pkgutil result interpretation and keeps present, absent, and read-failed receipt states distinct; HostCLI only supplies package identifiers and the concrete process runner.
- 2026-06-06: Moved install/uninstall workflow state artifact persistence out of `HostCLI/Runtime`. Workflow still owns operation transition decisions, while the outbound persistence adapter writes the concrete JSON workflow artifact through explicit file-writing ports; HostCLI only wires installed paths and file-store adapters. These artifacts are workflow evidence, not Runtime Control current status owners.
- 2026-06-06: Moved `RuntimeVersionStore`, `RuntimeVersionStoreMetadata`, and `RuntimeVersionReadResult` from `HostCLI/Runtime` into `Infrastructure/Repositories`. Runtime version document persistence now receives product/artifact names as explicit metadata instead of reading HostCLI constants, preserving missing, loaded, and invalid version states as repository output.
- 2026-06-06: Moved `RuntimeBackupStore`, managed backup artifact mapping, paths, metadata, and backup-store error from `HostCLI/Runtime` into `Infrastructure/Repositories`. Backup manifest creation and latest-backup selection now receive product/artifact/tool paths as explicit metadata and report repository errors instead of throwing HostCLI `LauncherError`; HostCLI only supplies concrete paths, file operations, chmod, and logging.
- 2026-06-06: Moved `RuntimeVMLifecycleStore` from `HostCLI/Runtime` into `Infrastructure/Repositories`. VM lifecycle document load/write state now lives with persisted runtime repositories, keeps missing and decode/read failure explicit through `RuntimeGuestDocumentLoadResult`, and uses an Infrastructure-local encoder instead of HostCLI JSON helpers.
- 2026-06-06: Moved `RuntimeGuestRuntimeStateObservationReader` from `HostCLI/Runtime` into `Workflow/RuntimeHealth`. Guest runtime-state freshness assembly now consumes explicit `RuntimeGuestGateway` and file metadata ports in Workflow, while HostCLI only wires concrete gateway, file store, runtime-state URL, staleness window, and clock.
- 2026-06-06: Moved `RuntimeLogRotator` and `RuntimeLogRotationConfiguration` from `HostCLI/Runtime` into `Infrastructure/FileSystem`. Log rotation now receives log file names, size threshold, and retention count explicitly from HostCLI composition instead of reading HostCLI constants inside filesystem maintenance code.
- 2026-06-06: Moved `RuntimeStorageMaintenance`, its configuration, and its maintenance error type from `HostCLI/Runtime` into `Infrastructure/FileSystem`. Free-space checks, runtime artifact pruning, and atomic file replacement now receive retention policy and filesystem ports explicitly, and no longer throw HostCLI `LauncherError` or read HostCLI constants inside Infrastructure.
- 2026-06-06: Moved `RuntimeGuestLogCollector` from `HostCLI/Runtime` into `Infrastructure/FileSystem`. Guest log sync, append validation, rotated log copy, and central-log archiving now live with filesystem infrastructure over explicit installed paths and file-store ports; HostCLI keeps only a typealias shim.
- 2026-06-06: Moved `RuntimeInstallSettingsStateReader` and `RuntimeInstallArtifactStateReader` from `HostCLI/Runtime` into `Infrastructure/FileSystem`. Fresh-install settings and artifact inspection now preserve missing/defaulted/loaded/invalid/read-failed and absent/present/inspect-failed states in Infrastructure while receiving the documented default proxy port explicitly from HostCLI.
- 2026-06-06: Moved `RuntimeConfigFlagReader` and `RuntimeConfigFlagValues` from `HostCLI/Runtime` into `Interfaces/HostCLI`. Runtime config flag defaulting/logging now consumes explicit flag values instead of depending on `VMRuntimeConfig`, while HostCLI remains responsible for reading the concrete VM config file and mapping it into the interface DTO.
- 2026-06-06: Moved installer settings input mapping into `Interfaces/HostCLI` as `RuntimeInstallSettings`, `RuntimeInstallSettingsDefaults`, and `RuntimeInstallSettingsError`. HostCLI now supplies product defaults and maps interface missing-argument errors to `LauncherError`, while the settings decoder/validator no longer imports HostCLI constants.
- 2026-06-06: Moved `RuntimeServiceControlCommand` from `HostCLI/Runtime` into `Interfaces/HostCLI`. Service-control CLI command values now live at the CLI interface boundary, while HostCLI's runner only maps those explicit commands into Workflow lifecycle commands.
- 2026-06-06: Moved `RuntimeConfigureCommand` and `RuntimeConfigureChange` from `HostCLI/Runtime` into `Interfaces/HostCLI`, and moved `RuntimeNetworkMode` into `Contracts`. Configure command parsing now produces explicit CLI interface command values over shared network-mode vocabulary, while HostCLI keeps only compatibility aliases plus concrete VM config file ownership.
- 2026-06-06: Moved `RuntimeLifecycleCommand` from `HostCLI/Runtime` into `Interfaces/HostCLI`. The command value moved first, while the argv parser stayed temporarily in HostCLI until the later parser slice moved it to the CLI interface boundary.
- 2026-06-06: Moved `RuntimeManagedOperationGuard` and `RuntimeGuestBootstrapOperation` from `HostCLI/Runtime` into `Workflow/RuntimeWatchdog`. Watchdog recovery suppression now consumes explicit status reporter and guest-bootstrap inputs in Workflow, while HostCLI only wires the status repository, clock, bootstrap reader, grace window, and log sink.
- 2026-06-06: Moved `RuntimeStatusPrinter` from `HostCLI/Runtime` into `Interfaces/HostCLI`. CLI status output formatting now consumes explicit path, status, service, file, and HTTP values without reading HostCLI constants; HostCLI only injects concrete paths and state readers.
- 2026-06-06: Moved `RuntimeGuestCapabilityChecker` and `RuntimeGuestCapabilityCheckError` from `HostCLI/Runtime` into `Workflow/RuntimeUpdateLifecycle`. Guest capability gates now consume explicit guest runtime-state document load results in Workflow, preserving missing/read-failed/capability-missing messages without throwing HostCLI `LauncherError`.
- 2026-06-06: Moved `ProcessState` and `ProcessStateError` from `HostCLI/Runtime` into `HostAdapters/Process`. VM pid-file reads/writes, process-existence probing, SIGTERM/SIGKILL dispatch, and stop polling now consume explicit Application file-store ports in HostAdapters and no longer import `Core`, `HostInfrastructure`, or throw HostCLI `LauncherError`.
- 2026-06-06: Moved `RuntimeHostProxyPortCleaner`, `RuntimeHostProxyPortCleanerError`, and `RuntimeProxyNginxPIDReadResult` from `HostCLI/Runtime` into `HostAdapters/Process`. Host proxy cleanup now receives lsof/ps/kill paths explicitly from HostCLI composition, keeps proxy PID read states distinct, and reports typed adapter failures instead of importing HostCLI constants or throwing `LauncherError`.
- 2026-06-06: Moved `RuntimeHostProxyPortStateReader` from `HostCLI/Runtime` into `HostAdapters/Process`. Fresh-install proxy port inspection now receives the lsof path explicitly and reports clear, occupied, and inspect-failed states without importing HostCLI constants.
- 2026-06-06: Moved `RuntimeServiceController` and `RuntimeServiceControllerError` from `HostCLI/Runtime` into `HostAdapters/Launchd`. Launchd service start/stop/enable orchestration now receives the launchctl path explicitly from HostCLI composition and reports typed adapter errors instead of importing HostCLI constants or throwing `LauncherError`.
- 2026-06-06: Moved `RuntimeCommandExecutor` and `RuntimeCommandExecutionError` from `HostCLI/Runtime` into `Workflow/RuntimeShared`. Command execution orchestration now runs over the explicit `RuntimeCommandRunner` port and records command lifecycle events in Workflow, while concrete process execution stays in HostAdapters and HostCLI keeps only a typealias shim.
- 2026-06-06: Moved `RuntimeHealthCheckRunner` and `RuntimeHealthCheckRunnerError` from `HostCLI/Runtime` into `Interfaces/HostCLI`. Health-check CLI output orchestration now consumes explicit health snapshot/status/event/printing closures in Interfaces and no longer throws HostCLI `LauncherError`.
- 2026-06-06: Moved `RuntimeHealthWaitRunner` and `RuntimeHealthWaitRunnerError` from `HostCLI/Runtime` into `Interfaces/HostCLI`. Health-wait CLI orchestration now receives `RuntimeHealthWaitWorkflowConfiguration` explicitly from HostCLI composition instead of reading HostCLI constants inside the interface layer, and no longer throws HostCLI `LauncherError`.
- 2026-06-06: Moved `RuntimeServiceControlRunner` from `HostCLI/Runtime` into `Interfaces/HostCLI`. Service-control CLI dispatch now lives with the service-control command value and calls `RuntimeServiceLifecycleWorkflow` through explicit service/status/log closures while HostCLI keeps only a typealias shim.
- 2026-06-06: Moved `RedisBackupResultReader` from `HostCLI/RuntimeLifecycle` into `Infrastructure/Repositories`. Redis backup result loading now lives with repository/file document readers, preserving missing, loaded, and decode/read-failed states as explicit workflow input while HostCLI keeps only a typealias shim.
- 2026-06-06: Moved `RuntimeGuestConfigWriter` from `HostCLI/Runtime` into `Infrastructure/FileSystem`. The writer now only persists an explicit `GuestRuntimeConfigDocument` plus its settings wrapper and restricts the secret file; HostCLI install composition remains responsible for mapping install settings and product constants into that explicit document.
- 2026-06-06: Moved `LauncherPaths` from `HostCLI/Runtime` into `Bootstrap/Composition`. Runtime home resolution now receives the VM-home environment key and default home path components explicitly, while HostCLI keeps a compatibility adapter that supplies product constants for the CLI entrypoint.
- 2026-06-06: Moved `RuntimeLifecycleCommand` argv parsing, usage text, and `RuntimeLifecycleCommandParseError` into `Interfaces/HostCLI`. HostCLI now keeps only the compatibility `parse`/`usage` extension that maps explicit interface parse errors into existing `LauncherError` values for the CLI entrypoint.
- 2026-06-06: Moved `RuntimeHealthChecker` into `Infrastructure/Health` behind an explicit `RuntimeHealthCheckerContext`. Health snapshot assembly now receives executable paths, command paths, URL builders, default proxy port, and staleness windows from HostCLI composition instead of reading HostCLI constants directly. `RuntimeProxyNginxPIDReadResult` moved to `Contracts` because both host proxy cleanup and health snapshot assembly consume the same explicit missing/empty/loaded/read-failed PID state.
- 2026-06-06: Moved guest runtime config document reading into `Infrastructure/FileSystem` as `RuntimeGuestConfigDocumentReader` with a typed missing-file error. HostCLI now maps that explicit infrastructure read error to `LauncherError.missingFile`, and product default guest config fixture creation is no longer hidden on `GuestRuntimeConfigDocument`.
- 2026-06-06: Moved `VMRuntimeConfig`, `NetworkConfig`, and `SharedDirectoryConfig` into `HostAdapters/VirtualMachine` with typed config-read and boot-file validation errors. HostCLI keeps product default construction, local MAC prefix injection, and `LauncherError` translation as compatibility shim behavior, so future configure/install composition can depend on a host adapter document type instead of a HostCLI-owned schema.
- 2026-06-06: Moved `VMConfigurationFactory` into `HostAdapters/VirtualMachine` with typed Virtualization adapter errors. HostCLI now supplies the detached-process fact, standard serial handles, and existing `LauncherError` translation at the process boundary instead of owning the Virtualization.framework object graph.
- 2026-06-06: Moved `VirtualMachineDelegate` and `VirtualMachineTerminationHandler` into `HostAdapters/VirtualMachine`. The adapters now receive lifecycle writes, process exit, and output/error sinks explicitly, while HostCLI owns only the Infrastructure `RuntimeVMLifecycleStore` wiring and process-boundary defaults.
- 2026-06-06: Moved top-level HostCLI `Command` parsing into `Interfaces/HostCLI`. The process entrypoint now consumes an explicit CLI interface vocabulary, while HostCLI keeps only a typealias shim for the executable target.
- 2026-06-06: Moved `LauncherError` into `Interfaces/HostCLI`. CLI error vocabulary and human-readable descriptions now live with the CLI interface boundary, while final Workflow/Infrastructure/HostAdapters boundary tests continue to prevent inward layers from depending on that presentation error type.
- 2026-06-06: Moved runtime `Constants` and generated helper version constants from `HostCLI/Runtime` into `Bootstrap/Composition`. Product paths, command paths, runtime default values, helper version/channel, and testkit build flag are now owned by the composition root, with HostCLI retaining only a compatibility alias.
- 2026-06-06: Moved `RuntimeLifecycleComposition` from `HostCLI/Runtime` into `Bootstrap/Composition`. The lifecycle facade now consumes Bootstrap-provided wiring for HTTP probing, launchd service management/control, status reporting, health snapshot assembly, and guest gateway construction while HostCLI keeps only a compatibility alias.
- 2026-06-06: Moved `RuntimeBundleComposition` from `HostCLI/Runtime` into `Bootstrap/Composition`. Update bundle verification/staging/apply wiring now lives in the composition root and joins Workflow update lifecycle components to explicit host file/process/storage/status ports, while HostCLI keeps compatibility aliases for the context, operations, and composition types.
- 2026-06-06: Moved `RuntimeInstallComposition` from `HostCLI/Runtime` into `Bootstrap/Composition`. Install usecase/workflow wiring now joins Workflow install lifecycle components to explicit host file/process/launchd/cloud-init/VM-config/status ports in the composition root, while HostCLI keeps compatibility aliases for the context, operations, and composition types.
- 2026-06-06: Moved `RuntimeConfigureRunner` from `HostCLI/Runtime` into `Bootstrap/Composition`. Configure usecase/workflow wiring now joins the CLI configure command to explicit VM config file, guest config file, service restart, start-on-boot, sleep-prevention, disk-resize, and proxy-port effects at the composition root. HostCLI keeps only aliases, which prevents the CLI facade from silently becoming the owner of configure operation wiring again.
- 2026-06-06: Moved runtime managed-service launchd plist path composition into `Bootstrap/Composition/RuntimeManagedServicePaths`. `RuntimeLifecycleComposition` and `RuntimeInstallComposition` now share the same explicit helper, and HostCLI's `RuntimeManagedService` extension only delegates to Bootstrap. This prevents product install-path constants from being reintroduced in HostCLI support extensions.
- 2026-06-06: Moved health checker product context composition into `Bootstrap/Composition/RuntimeHealthCheckerComposition`. HostCLI still exposes the compatibility `RuntimeHealthChecker(installedPaths:...)` convenience initializer for tests and callers, but it delegates to Bootstrap instead of reading product constants, default proxy ports, URL builders, or launchd plist paths in the CLI layer.
- 2026-06-06: Moved VM runtime config product defaults, runtime-default backfill, boot-file validation error mapping, and pretty JSON encoder selection into `Bootstrap/Composition/VMRuntimeConfigComposition`. HostCLI's `VMRuntimeConfig` extension now delegates to Bootstrap, and configure/install composition use the same helper instead of duplicating product constants and MAC identity injection rules.
- 2026-06-06: Moved Redis backup workflow composition into `Bootstrap/Composition/RuntimeRedisBackupComposition`. Symptom: HostCLI still owned guest request/result file names, Redis backup JSON encoding, result-reader selection, status writer wiring, VM service gate, and polling sleep composition even after `RuntimeRedisBackupWorkflow` moved to final Workflow. Cause: workflow migration moved operation ordering first, but left product contract and adapter wiring in the CLI facade. Fix direction: Bootstrap now composes the Redis backup workflow over explicit Host-provided ports, while HostCLI keeps only compatibility aliases and concrete closure injection. Prevention: when moving an operation workflow, also move request/result file contract wiring and adapter selection to Bootstrap so the CLI cannot silently become the operation composition owner again.
- 2026-06-06: Moved cloud-init seed writer product composition into `Bootstrap/Composition/RuntimeCloudInitSeedComposition`. Symptom: HostCLI and install composition duplicated seed image name, `cidata` volume, hdiutil path, file-store operation mapping, and instance-id generation while the writer implementation already lived in `HostAdapters/CloudInit`. Cause: the adapter move separated the effect implementation but left product context assembly scattered across callers. Fix direction: Bootstrap now creates the `RuntimeCloudInitSeedWriter` from explicit file-store/process ports and product constants; HostCLI keeps only a compatibility alias and install composition reuses the same helper. Prevention: host adapter migrations should move both the adapter implementation and its product-specific composition contract, otherwise duplicated constants reappear at operation call sites.
- 2026-06-06: Moved service-control runner composition into `Bootstrap/Composition/RuntimeServiceControlComposition`. Symptom: HostCLI still assembled `RuntimeServiceControlRunner` directly and owned the bulk service-state dictionary mapping from per-service launchd reads. Cause: the service-control runner had moved to the CLI interface layer, but its concrete runtime ports were still being wired in the CLI facade. Fix direction: Bootstrap now joins start/stop effects, service-state reads, status writing, and logging into the interface runner; HostCLI only supplies concrete closures. Prevention: interface runners that execute runtime operations should be created by Bootstrap, while HostCLI remains the process entrypoint and compatibility shim.
- 2026-06-06: Moved status-printer product composition into `Bootstrap/Composition/RuntimeStatusPrinterComposition`. Symptom: HostCLI still created `RuntimeStatusPrinter` directly and supplied product executable paths plus the proxy health URL builder even after the printer moved to `Interfaces/HostCLI`. Cause: output formatting moved first, but product path and URL composition stayed in the CLI facade. Fix direction: Bootstrap now creates the printer from explicit status/version/service/file ports plus product constants; HostCLI only supplies current state closures. Prevention: once a CLI formatter lives in Interfaces, any product path, URL, or installed-path assembly required to create it should live in Bootstrap.
- 2026-06-06: Moved health-check runner composition into `Bootstrap/Composition/RuntimeHealthCheckRunnerComposition`. Symptom: HostCLI still called workflow best-effort status/event recording helpers and selected the `.healthObserved` event type while creating the `RuntimeHealthCheckRunner`. Cause: the interface runner moved out, but operation-specific recording composition remained in the CLI facade. Fix direction: Bootstrap now joins the runner to health snapshots, status writes, observed-event writes, and best-effort helper policies; HostCLI only supplies concrete publisher/status closures. Prevention: best-effort recording wrappers around workflow/interface runners belong at the composition root, not inside HostCLI command wiring.
- 2026-06-06: Moved managed-operation guard composition into `Bootstrap/Composition/RuntimeManagedOperationGuardComposition`. Symptom: HostCLI still assembled `RuntimeManagedOperationGuard` directly and owned guest bootstrap result loading, runtime-state bootID stale-result rejection, ISO8601 timestamp parsing, and watchdog managed-operation grace-window constants. Cause: the guard type moved to Workflow, but the guest bootstrap guard composition stayed in the CLI facade. Fix direction: Bootstrap now composes the guard over explicit status reporter, guest gateway, clock, and log ports; HostCLI only supplies those concrete collaborators. Prevention: operation suppression/recovery guards should keep pure decision code in Workflow/Domain and put guest/status document composition in Bootstrap, not in HostCLI.
- 2026-06-06: Moved watchdog runner composition into `Bootstrap/Composition/RuntimeWatchdogRunnerComposition`. Symptom: HostCLI still created `RuntimeWatchdogRunner` directly and owned log preparation best-effort handling, proxy liveness URL composition, VM lifecycle store write wiring, recovery wait constants, and status/event recording order. Cause: the watchdog workflow had moved to final Workflow, but its product runtime composition stayed in the CLI lifecycle facade. Fix direction: Bootstrap now composes the runner over explicit file-store, health, HTTP, service restart, lifecycle, status, event, sleep, log, and print ports; HostCLI supplies only concrete closures. Prevention: after moving a runner to Workflow, move any product constants, URL builders, persistence adapter selection, and best-effort recording wrappers to Bootstrap in the same slice.
- 2026-06-06: Moved guest capability checker composition into `Bootstrap/Composition/RuntimeGuestCapabilityCheckerComposition`. Symptom: HostCLI still created `RuntimeGuestCapabilityChecker` directly and captured `guestGateway.loadRuntimeStateDocument()` inside the lifecycle facade. Cause: the checker implementation moved to Workflow, but the guest runtime-state read port wiring remained in HostCLI. Fix direction: Bootstrap now composes the checker over the explicit guest gateway contract, and HostCLI only invokes the composed checker for a requested capability. Prevention: workflow guards should receive external state through Bootstrap-composed ports so HostCLI cannot become the hidden owner of guest document reads.
- 2026-06-06: Moved datastore repair workflow composition into `Bootstrap/Composition/RuntimeDatastoreRepairComposition`. Symptom: HostCLI still assembled `RuntimeDatastoreRepairWorkflow` directly and owned guest result cleanup, guest request writing, guest result loading, wait timeout constants, and VM/proxy/watchdog service port wiring. Cause: the repair workflow moved to final Workflow before its concrete guest-gateway composition moved out of the CLI lifecycle facade. Fix direction: Bootstrap now joins datastore repair workflow context to explicit file-store, guest gateway, service, status, sleep, timestamp, and log ports; HostCLI supplies only concrete collaborators. Prevention: repair workflows should move guest request/result adapter selection and timeout constants to Bootstrap with the workflow migration, not as a later hidden HostCLI responsibility.
- 2026-06-06: Moved VM disk repair runner composition into `Bootstrap/Composition/RuntimeVMDiskRepairComposition`. Symptom: HostCLI still assembled `RuntimeVMDiskRepairRunner` directly and owned rootfs/disk artifact path construction, default disk size, free-space margin, gunzip/truncate command paths, and filesystem operation mapping. Cause: the runner moved to final Workflow, but product runtime path/default composition stayed in the CLI lifecycle facade. Fix direction: Bootstrap now creates the runner from installed paths, product constants, file-store ports, process ports, repair backup/service/health effects, status writer, timestamp, and log. Prevention: repair runners should keep replacement order in Workflow and move product artifact/default/command wiring to Bootstrap.
- 2026-06-06: Moved guest update activation/shutdown workflow composition into `Bootstrap/Composition/RuntimeGuestActivationComposition` and `RuntimeGuestShutdownComposition`. Symptom: HostCLI still assembled `RuntimeGuestActivationWorkflow` and `RuntimeGuestShutdownWorkflow` directly, including guest result cleanup, request writing, result loading, wait timeout constants, VM service gate wiring, and polling/status ports. Cause: the guest update workflows moved to final Workflow before their concrete guest-gateway composition moved out of the CLI lifecycle facade. Fix direction: Bootstrap now joins guest activation/shutdown workflows to explicit file-store, guest gateway, capability, VM service, status, request metadata, sleep, and log ports; HostCLI supplies only concrete collaborators. Prevention: guest request/result orchestration should keep evaluation and timeout behavior in Workflow/Domain while Bootstrap owns adapter selection and product timeout constants.
- 2026-06-06: Moved uninstall workflow composition into `Bootstrap/Composition/RuntimeUninstallComposition`. Symptom: HostCLI still assembled `RuntimeUninstallWorkflow` directly and owned uninstall path construction, package receipt reads/forget commands, cleanup artifact state reads, VM process inspection, uninstall state store selection, service disable/stop/proxy cleanup ordering, and open-file diagnostics wiring. Cause: the uninstall workflow moved to final Workflow, but its concrete host-state/effect composition stayed in the lifecycle support facade. Fix direction: Bootstrap now composes uninstall over installed paths, pid file, file-store ports, package/process adapters, service effects, state writer, diagnostics, and product constants; HostCLI supplies only concrete collaborators. Prevention: workflows with persisted operation state should move repository/adapter selection and product command constants to Bootstrap with the workflow, leaving transition policy in Domain and step orchestration in Workflow.
- 2026-06-06: Moved rollback workflow composition into `Bootstrap/Composition/RuntimeRollbackComposition`. Symptom: HostCLI still assembled `RuntimeRollbackWorkflow` directly and owned rollback target path construction, manager/nginx/deploy restore target selection, file-store read mapping, backup restore port selection, and status/progress wiring. Cause: rollback orchestration had moved to final Workflow, but product path and adapter wiring remained in the CLI lifecycle facade. Fix direction: Bootstrap now creates the rollback workflow from installed paths, product constants, file-store ports, backup restore ports, service health ports, status/progress writers, and log. Prevention: update/rollback workflows should move target path and backup adapter composition to Bootstrap so HostCLI remains command routing and concrete collaborator injection only.
- 2026-06-06: Moved workflow status reporter composition into `Bootstrap/Composition/RuntimeWorkflowStatusReporterComposition`. Symptom: HostCLI still instantiated `RuntimeWorkflowStatusReporter` directly even after the reporter implementation moved to `Workflow/RuntimeShared`, leaving status/progress writer wiring in the lifecycle facade. Cause: shared workflow reporting policy migrated first, but its concrete writer-port composition stayed in HostCLI as a private helper. Fix direction: Bootstrap now creates the reporter from explicit status writer, progress writer, and log ports; HostCLI keeps only a compatibility alias and supplies concrete closures. Prevention: shared Workflow helpers that bridge to concrete status/progress/event stores should have a Bootstrap composition boundary, so HostCLI does not become the hidden owner of writer selection.
- 2026-06-06: Moved status writer composition into `Bootstrap/Composition/RuntimeStatusWriterComposition`. Symptom: HostCLI still instantiated `RuntimeStatusWriter` directly and selected reporter, timestamp, runtime version, health snapshot, and latest-backup ports in a support helper. Cause: status document writing moved to final Workflow, but writer construction stayed in the CLI facade. Fix direction: Bootstrap now creates the writer from explicit reporter/timestamp/version/snapshot/backup ports; HostCLI keeps only a compatibility alias and concrete closure injection. Prevention: shared status writers should be composed at Bootstrap whenever they bind workflow reporting to host-owned read models.
- 2026-06-06: Moved health-wait runner composition into `Bootstrap/Composition/RuntimeHealthWaitRunnerComposition`. Symptom: HostCLI still created `RuntimeHealthWaitRunner` directly and owned timeout, poll interval, progress cadence, service-state dictionary mapping, best-effort status write wrapper, and sleep interval. Cause: the runner moved to `Interfaces/HostCLI`, but runtime operation configuration and best-effort workflow helper wiring remained in HostCLI. Fix direction: Bootstrap now joins the interface runner to health-wait configuration, service state, health snapshot, status writer, sleep, and log ports; HostCLI supplies only concrete closures. Prevention: interface runners that execute workflow waits should move operation constants and best-effort workflow helpers to Bootstrap with the runner migration.
- 2026-06-06: Moved fresh-install preflight composition into `Bootstrap/Composition/RuntimeFreshInstallPreflightComposition`. Symptom: HostCLI still assembled `RuntimeFreshInstallPreflightRunner` directly and owned install settings path/default proxy port, product artifact path list, package receipt identifiers, lsof path, and concrete state-reader selection. Cause: the preflight runner moved to final Workflow before product path and adapter selection moved out of the CLI support facade. Fix direction: Bootstrap now creates the runner from installed paths, file-store, service-state, process-runner, product constants, and concrete settings/artifact/receipt/proxy-port readers; HostCLI keeps only compatibility aliases and concrete closure injection. Prevention: preflight workflows should move Host-owned state reader selection and product artifact lists to Bootstrap so HostCLI cannot recreate domain preflight state from local constants.
- 2026-06-06: Moved backup/version repository composition into `Bootstrap/Composition/RuntimeBackupStoreComposition` and `RuntimeVersionStoreComposition`. Symptom: HostCLI support still created Infrastructure repositories directly and owned backup paths, runtime version file path, product artifact names, runtime tool paths, chmod command selection, and metadata constants. Cause: repository implementations moved to Infrastructure, but product-specific repository construction stayed in the CLI facade. Fix direction: Bootstrap now creates backup/version stores from installed paths, product constants, file-store ports, timestamp closures, chmod command, and log sink; HostCLI keeps only compatibility aliases and concrete closure injection. Prevention: when a repository implementation moves to Infrastructure, its product metadata/path composition should move to Bootstrap rather than remain in HostCLI support helpers.
- 2026-06-06: Tightened configure runner composition with `RuntimeConfigureCompositionContext` and `RuntimeConfigureCompositionOperations`. Symptom: `RuntimeConfigureRunner` lived in Bootstrap, but `RuntimeLifecycle` still directly constructed the runner and `RuntimeConfigureActions`, leaving configure effect wiring visually owned by HostCLI. Cause: the first configure migration moved the runner implementation but did not give the lifecycle facade the same context/operations composition shape used by later runtime operations. Fix direction: Bootstrap now owns the configure runner/action bundle construction, while HostCLI supplies installed paths, config URL, file-store, and concrete effect closures. Prevention: after moving an operation runner to Bootstrap, give it an explicit composition entry point so HostCLI cannot keep partial assembly responsibility by directly calling the runner initializer.
- 2026-06-13: Tightened service-control VM ownership. Symptom: service-control callers could visually pass `startRuntimeServicesForServiceControl` and `stopRuntimeServicesForServiceControl` wrappers into the runner, leaving the owner boundary easy to bypass at call sites. Cause: `RuntimeVMStateControlUseCase` existed, but service-control composition did not itself express that all VM start/stop effects must pass through the owner. Fix direction: `RuntimeServiceControlComposition` now wraps raw start/stop effects with `RuntimeVMStateControlUseCase` before constructing the runner, and an architecture test pins the boundary. Prevention: CLI/service-control composition should receive host effects, but VM transition ownership must be expressed in the composition entrypoint, not only by naming helper closures at individual call sites.
- 2026-06-13: Added broader architecture guards for VM state-control ownership. Symptom: even after service-control composition used the owner, future code could still instantiate `RuntimeVMStateControlUseCase` in scattered call sites or call VM service-controller effects directly outside the lifecycle bridge. Cause: owner intent was documented but not statically guarded. Fix direction: architecture tests now allow `RuntimeVMStateControlUseCase()` only in the approved Host boundaries and keep direct VM `serviceController` effects inside `RuntimeLifecycle+ServiceSupport.swift`. Prevention: new Settings, update, repair, watchdog, or service-control VM effects must add an explicit owner method or approved bridge instead of creating a new direct launchd path.
