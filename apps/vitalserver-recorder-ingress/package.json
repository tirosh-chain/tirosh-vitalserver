{
  "name": "@tirosh/vitalserver-recorder-ingress",
  "version": "0.1.0",
  "private": true,
  "description": "VitalServer recorder ingress, Socket.IO audit, and edge-observation sidecar",
  "type": "commonjs",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "test": "node --test tests/*.test.js",
    "check": "find . -name '*.js' -print -exec node --check {} \\;"
  },
  "engines": {
    "node": ">=20"
  }
}
