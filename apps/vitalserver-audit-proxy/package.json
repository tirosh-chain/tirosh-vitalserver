{
  "name": "@tirosh/vitalserver-audit-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "VitalServer Socket.IO audit and edge-observation proxy",
  "type": "commonjs",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "test": "node --test tests/*.test.js",
    "check": "find . -name '*.js' -print -exec node --check {} \\;"
  },
  "engines": {
    "node": ">=20"
  }
}
