# Runtime PWA

The Runtime PWA renders explicit read models and submits commands to the public control facade.

It does not infer runtime state, own domain transition rules, read host files, or call private service endpoints directly.
