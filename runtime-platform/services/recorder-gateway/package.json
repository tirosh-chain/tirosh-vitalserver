{
  "name": "@tirosh-chain/recorder-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Socket.IO v2-compatible Recorder ingress with explicit durable and delivery receipts",
  "type": "module",
  "engines": {
    "node": ">=20.19.0 <21"
  },
  "scripts": {
    "build": "node ./scripts/clean-recorder-gateway-build-output.mjs && tsc --project tsconfig.json",
    "check": "tsc --project tsconfig.json --noEmit",
    "test": "npm run build && find src/test -name '*.test.ts' -print | sort | sed 's#^src/#dist/#; s#\\.ts$#.js#' | xargs node --test"
  },
  "dependencies": {
    "socket.io": "4.8.3",
    "socket.io-client": "4.8.3"
  },
  "devDependencies": {
    "@types/node": "20.19.43",
    "typescript": "5.9.3",
    "ws": "8.21.1"
  }
}
